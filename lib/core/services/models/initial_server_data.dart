import 'package:pigallery2_android/core/services/models/session_data.dart';

class InitialServerData {
  String? serverUrl;
  SessionData? sessionData;
  InitialServerData({this.serverUrl, this.sessionData});
}
